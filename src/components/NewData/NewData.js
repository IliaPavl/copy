import React from 'react';
import { Card } from 'react-bootstrap';

const NewData = () => {
    return (
        <div>
            <Card>New data</Card>
        </div>
    );
};

export default NewData;